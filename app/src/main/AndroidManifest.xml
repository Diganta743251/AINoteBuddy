<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    
    <!-- Camera and media permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    
    <!-- Location permissions for context-aware features -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Voice and speech permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Boot completed to reschedule reminders -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    
    <!-- Calendar integration -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    
    <!-- Contacts for sharing -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Wake lock for background sync -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Foreground service for sync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    
    <!-- Hardware features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.location" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="true" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <application
        android:name=".AINoteBuddyApplication"
        android:theme="@style/Theme.AINoteBuddy"
        android:label="AINoteBuddy"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        xmlns:tools="http://schemas.android.com/tools">

        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${admob_app_id}" />

        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />

        <activity 
            android:name=".MainActivity" 
            android:exported="true"
            android:theme="@style/Theme.AINoteBuddy"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".auth.LoginActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy" />
        
        <activity 
            android:name=".features.NoteEditorActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".features.SearchActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".features.SettingsActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".features.TemplatesActivity"
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".features.VoiceRecorderActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".features.AISettingsActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:parentActivityName=".MainActivity" />
            
        <activity 
            android:name=".features.ApiKeySettingsActivity" 
            android:exported="false"
            android:theme="@style/Theme.AINoteBuddy"
            android:parentActivityName=".features.AISettingsActivity" />
            
        <activity
            android:name=".features.ShareReceiverActivity"
            android:exported="true"
            android:theme="@style/Theme.AINoteBuddy">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        
        <!-- File provider for camera and file sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Google Drive sync service -->
        <service
            android:name=".sync.GoogleDriveSyncServiceImpl"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
            
        <!-- AI processing service -->
        <service
            android:name=".ai.AIProcessingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
            
        <!-- Biometric authentication -->
        <service
            android:name=".security.BiometricAuthService"
            android:exported="false" />
            
        <!-- Location service for context-aware features -->
        <service
            android:name=".location.LocationService"
            android:exported="false" />
            
        <!-- Voice recognition service -->
        <service
            android:name=".voice.VoiceRecognitionService"
            android:exported="false" />

        <receiver android:name=".StickyNoteWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Boot receiver to reschedule reminders -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>



    </application>
</manifest>
