package com.ainotebuddy.app.ui.theme

/**
 * UI note type representing color/theme categories used by editor and list screens.
 * This is distinct from search.NoteType (TEXT, VOICE, etc.).
 */
enum class NoteType {
    PERSONAL,
    WORK,
    IDEA,
    TASK,
    ARCHIVE
}
